# LMCacheEngine on AMD ROCm GPU nodes.
apiVersion: lmcache.lmcache.ai/v1alpha1
kind: LMCacheEngine
metadata:
  name: amd-cache
spec:
  gpuVendor: amd
  nodeSelector:
    # Use the label your platform exposes for AMD GPU nodes.
    feature.node.kubernetes.io/amd-gpu: "true"
  l1:
    sizeGB: 60
