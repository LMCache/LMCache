# The Buildkite UI pipeline still uploads this file. Keep a skipped placeholder
# so the upload succeeds without scheduling unit tests.
steps:
  - label: ":no_entry: Unit tests disabled"
    if: "false"
    command: "true"
