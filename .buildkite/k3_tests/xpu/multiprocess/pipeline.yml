# XPU multiprocess tests.
# This suite reuses the multiprocess scripts through an XPU wrapper and may
# allocate 1 or 2 Intel GPUs depending on the test step.

x-flaky-retry: &flaky-retry
  automatic:
    - exit_status: -1
      limit: 2
    - exit_status: "*"
      limit: 2

x-pod-1xpu: &pod-1xpu
  containers:
    - name: container-0
      image: public.ecr.aws/q9t5s3a7/vllm-ci-test-repo:ee0da84ab9e04ac7610e28580af62c365e898389-xpu
      imagePullPolicy: IfNotPresent
      resources:
        requests:
          deviceclass.resource.kubernetes.io/gpu.intel.com: "1"
        limits:
          deviceclass.resource.kubernetes.io/gpu.intel.com: "1"
      volumeMounts:
        - { name: hf-cache, mountPath: /root/.cache/huggingface }
        - { name: dshm, mountPath: /dev/shm }
  volumes:
    - { name: hf-cache, hostPath: { path: /data/huggingface, type: DirectoryOrCreate } }
    - { name: dshm, emptyDir: { medium: Memory, sizeLimit: 40Gi } }

x-pod-2xpu: &pod-2xpu
  containers:
    - name: container-0
      image: public.ecr.aws/q9t5s3a7/vllm-ci-test-repo:ee0da84ab9e04ac7610e28580af62c365e898389-xpu
      imagePullPolicy: IfNotPresent
      resources:
        requests:
          deviceclass.resource.kubernetes.io/gpu.intel.com: "2"
        limits:
          deviceclass.resource.kubernetes.io/gpu.intel.com: "2"
      volumeMounts:
        - { name: hf-cache, mountPath: /root/.cache/huggingface }
        - { name: dshm, mountPath: /dev/shm }
  volumes:
    - { name: hf-cache, hostPath: { path: /data/huggingface, type: DirectoryOrCreate } }
    - { name: dshm, emptyDir: { medium: Memory, sizeLimit: 40Gi } }

x-pod-4xpu: &pod-4xpu
  containers:
    - name: container-0
      image: public.ecr.aws/q9t5s3a7/vllm-ci-test-repo:ee0da84ab9e04ac7610e28580af62c365e898389-xpu
      imagePullPolicy: IfNotPresent
      resources:
        requests:
          deviceclass.resource.kubernetes.io/gpu.intel.com: "4"
        limits:
          deviceclass.resource.kubernetes.io/gpu.intel.com: "4"
      volumeMounts:
        - { name: hf-cache, mountPath: /root/.cache/huggingface }
        - { name: dshm, mountPath: /dev/shm }
  volumes:
    - { name: hf-cache, hostPath: { path: /data/huggingface, type: DirectoryOrCreate } }
    - { name: dshm, emptyDir: { medium: Memory, sizeLimit: 40Gi } }

steps:
  - group: ":xpu: Multiprocess (1-pod)"
    steps:
      - label: ":xpu: lm_eval"
        command: |
          export LMCACHE_MP_TRANSFER_MODE="engine_driven"
          export L1_USE_LAZY="false"
          export ENFORCE_EAGER="1"
          export VLLM_USE_STANDALONE_COMPILE="0"
          export CPU_BUFFER_SIZE="32"
          .buildkite/k3_tests/xpu/multiprocess/run.sh lm_eval
        timeout_in_minutes: 45
        retry: *flaky-retry
        agents: { queue: "intel-xpu" }
        plugins: [{ kubernetes: { podSpec: *pod-1xpu } }]
        artifact_paths: ["*.log", "ci_results_*/**"]

      - label: ":xpu: vllm_bench"
        command: |
          export LMCACHE_MP_TRANSFER_MODE="engine_driven"
          export L1_USE_LAZY="false"
          export ENFORCE_EAGER="1"
          export VLLM_USE_STANDALONE_COMPILE="0"
          export CPU_BUFFER_SIZE="32"
          export VLLM_DISABLE_PREFIX_CACHING="true"
          export MODEL="Qwen/Qwen3-14B"
          export TENSOR_PARALLEL_SIZE="2"
          export GPU_FOR_VLLM="0,1"
          export GPU_FOR_BASELINE="2,3"
          export RANDOM_INPUT_LEN="10000"
          export MAX_WAIT_SECONDS="900"
          .buildkite/k3_tests/xpu/multiprocess/run.sh vllm_bench
        timeout_in_minutes: 45
        retry: *flaky-retry
        agents: { queue: "intel-xpu" }
        plugins: [{ kubernetes: { podSpec: *pod-4xpu } }]
        artifact_paths: ["*.log", "ci_results_*/**"]
