# The Buildkite UI pipeline still uploads this file. Keep a skipped placeholder
# so the upload succeeds without scheduling XPU unit tests.
steps:
  - label: ":no_entry: XPU unit tests disabled"
    if: "false"
    command: "true"
