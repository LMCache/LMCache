# The Buildkite UI pipeline still uploads this file. Keep a skipped placeholder
# so the upload succeeds without scheduling integration tests.
steps:
  - label: ":no_entry: Integration tests disabled"
    if: "false"
    command: "true"
