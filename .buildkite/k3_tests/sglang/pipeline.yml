# The Buildkite UI pipeline still uploads this file. Keep a skipped placeholder
# so the upload succeeds without scheduling SGLang tests.
steps:
  - label: ":no_entry: SGLang tests disabled"
    if: "false"
    command: "true"
