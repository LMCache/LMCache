# The Buildkite UI pipeline still uploads this file. Keep a skipped placeholder
# so the upload succeeds without scheduling CacheBlend tests.
steps:
  - label: ":no_entry: CacheBlend tests disabled"
    if: "false"
    command: "true"
