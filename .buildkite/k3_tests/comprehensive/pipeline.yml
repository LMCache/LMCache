# Path-filter shim. See correctness/pipeline.yml for the full explanation.
# Conditionally uploads steps.yml or skips the build.

steps:
  - label: ":mag: Path filter"
    command: bash .buildkite/k3_tests/common_scripts/upload-pipeline.sh .buildkite/k3_tests/comprehensive/steps.yml
    agents: { queue: "k8s" }
