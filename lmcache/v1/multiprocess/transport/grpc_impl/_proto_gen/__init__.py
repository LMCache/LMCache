# SPDX-License-Identifier: Apache-2.0
"""Generated protobuf descriptors for the multiprocess gRPC transport.

The ``../protos/*.proto`` schemas are the source of truth. Package builds
generate ``*_pb2.py`` modules in this package. Runtime request and response
payloads are the transport-neutral Python messages from ``rpc_messages/``;
the generated modules provide route and method descriptors only. Regenerate
the descriptors after changing a schema with::

    pip install -r requirements/proto.txt
    python -m lmcache.v1.multiprocess.transport.grpc_impl._proto_gen._generate
"""
