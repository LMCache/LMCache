# Generated gRPC stubs -- NOT checked into git.
#
# ``lmcache_mq.proto`` (under ../proto/) is the single source of truth.
# The *_pb2.py / *_pb2_grpc.py stubs are produced from it by _generate.py
# and are generated lazily on first import (see __init__.py), so they are
# ignored here. Regenerate manually with:
#   python -m lmcache.v1.multiprocess.transport.grpc_impl._proto_gen._generate
lmcache_mq_pb2.py
lmcache_mq_pb2_grpc.py
