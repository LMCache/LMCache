// SPDX-License-Identifier: Apache-2.0
syntax = "proto3";

package lmcache.mp;

import "common.proto";

service QStoreService {
  rpc RegisterQCache(TransportPayload) returns (TransportPayload);
  rpc UnregisterQCache(TransportPayload) returns (TransportPayload);
  rpc StoreQ(TransportPayload) returns (TransportPayload);
}
