// SPDX-License-Identifier: Apache-2.0
syntax = "proto3";

package lmcache.mp;

// gRPC needs protobuf descriptors to name services and methods.  LMCache's
// internal request payload is instead the canonical Python message encoded by
// the transport-neutral MessagePack serializer.
message TransportPayload {}
