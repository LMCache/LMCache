# LMCache config for CacheBlend E2E test on ROCm
chunk_size: 256
local_cpu: true
max_local_cpu_size: 5.0  # GB

# CacheBlend settings
blend_check_layers: [0, 1, 2]
blend_recompute_ratios: [0.3]

# Extra config
extra_config:
  enable_sparse: true
