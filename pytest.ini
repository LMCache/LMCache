[pytest]
log_cli = true
log_cli_level = INFO
markers =
    no_shared_allocator: Disable the shared-allocator monkeypatch for this test
    integration: Mark integration tests
    slow: Mark slow tests excluded from the default PR test run

